# themes/common.conf — intentionally minimal.
# Only settings that should be IDENTICAL across all themes.
# Theme-specific colors live in themes/<name>.conf.

general {
    gaps_in = 5
    gaps_out = 10
    border_size = 2
}
